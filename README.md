# Reliable-UDP
Reliable-UDP simulates TCP reliability over UDP in pure Python, featuring a 3-way handshake, stop-and-wait, checksums, retransmission, and sequence numbers. Built on top is a lightweight HTTP/1.0 server and client supporting GET and POST methods. 
